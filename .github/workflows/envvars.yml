on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Ops
        env:
          myvar: "Lagos"
        run: |
          echo "My awesome env var is: ${{env.myvar}}"
